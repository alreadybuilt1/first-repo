Frontend is everything in the browser. HTML is the structure. CSS is the look, Javascript is the behavior.
Backend is my code running on a server. It decides what to send back and handles anything a browser cannot be trusted with.
Database is where things are stored so they survive.
